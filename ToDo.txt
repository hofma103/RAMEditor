auto formatting with spaces when using -> <- to hint at loops (probably use this as a way to measure loop executions)
properly handle NullPointerExceptions etc in RAMMachine

font size of 10 looks really bad on Windows (probably take at least 12 as standard option)